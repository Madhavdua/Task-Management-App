{
  "scripts": {
    "front":"cd front && npm start",
    "back":"cd backend && npm run server",
    "dev": "concurrently \"npm run front\" \"npm run back\""
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "moment": "^2.29.4"
  }
}
